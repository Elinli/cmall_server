

pub async fn verify_token(token: String) -> Result<(), String> {
    // Implement token verification logic here
    // For example, you can use a third-party library like jsonwebtoken to verify the token
    // If the token is valid, return Ok(()), otherwise return Err(String)
    Ok(())
}